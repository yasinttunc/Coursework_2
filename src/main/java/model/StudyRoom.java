package model;

public class StudyRoom extends Resource {

    // Constructor
    public StudyRoom(String resourceId, String resourceName) {
        super(resourceId, resourceName, ResourceType.STUDY_ROOM);
    }
}